<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.0/phpunit.xsd"
         bootstrap="/src/localarena/module/test/IntegrationTestCase.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         requireCoverageMetadata="false"
         beStrictAboutOutputDuringTests="true"
         displayDetailsOnTestsThatTriggerWarnings="true">
    <testsuites>
        <testsuite name="LocalArena Tests">
            <directory>.</directory>
        </testsuite>
    </testsuites>
    <!--
        Defines which code coverage is measured against. This has no effect on
        normal runs; coverage is only collected when PHPUnit is invoked with a
        `--coverage-*` option AND a coverage driver is active (PCOV is installed
        in the testenv image but disabled by default). See the "Generating
        code-coverage reports" section of README.md.
    -->
    <source>
        <include>
            <directory>/src/localarena/module</directory>
        </include>
    </source>
</phpunit>
